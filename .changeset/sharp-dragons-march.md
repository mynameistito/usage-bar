---
"usage-bar-windows": patch
---

Pinned GitHub Actions workflows to exact commit SHAs and Rust 1.88.0 for more reproducible CI and release builds. Also fixed Amp response preview logging so non-ASCII UTF-8 responses cannot panic during debug output.
