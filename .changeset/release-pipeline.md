---
"usage-bar-windows": patch
---

Add automated release pipeline that builds and attaches Windows x64 and ARM64 NSIS/MSI installers to GitHub Releases. Also fixes version sync to keep `tauri.conf.json` in step with `package.json` and `Cargo.toml`.
