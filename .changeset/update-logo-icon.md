---
"usage-bar-windows": patch
---

Use pre-generated PNG/ICO icons derived from SVG logo, with larger, more prominent usage bars. Fix CI Rust cache never being saved by adding `save-always: true` to `Swatinem/rust-cache@v2`.
